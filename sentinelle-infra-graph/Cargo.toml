[package]
name = "sentinelle-infra-graph"
version = "0.1.0"
edition = "2021"

[dependencies]
sentinelle-domain = { path = "../sentinelle-domain" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
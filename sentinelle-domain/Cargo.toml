[package]
name = "sentinelle-domain"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = "1"
once_cell = "1"
regex = "1"
serde = { version = "1.0", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }